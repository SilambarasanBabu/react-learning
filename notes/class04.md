1 Jsx is not required for using react
2 If you don't use es6 in react there is a alternate way to perform those operation
3 You con write comment inside {/\* \*/}
4 React-fiber: It is complete rewrite of react that fixes few long standanding issues and its focus on animation and responsiveness
5 VDOM : virtual represtation of UI that kept in memory which synced with the real dom this process is known as reconciliation
6 key attribute is used to idetify list which are changed,updated and removed
7 It is not recommended to use index as a key
